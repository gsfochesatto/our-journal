rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents in the entries collection
    // This is safe because the app is password protected at the UI level
    match /entries/{entryId} {
      allow read, write: if true;
    }
  }
}
